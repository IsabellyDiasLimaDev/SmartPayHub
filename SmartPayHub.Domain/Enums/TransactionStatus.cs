namespace SmartPayHub.Domain.Enums
{
    public enum TransactionStatus
    {
        Aprovada = 1, // Approved
        Pendente = 2, // Pending
        Cancelada = 3, // Canceled
        Recusada = 4, // Reversed
        Estornada = 5, // Refunded
    }
}
